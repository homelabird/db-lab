Target: {{ .Release.Namespace }} / {{ .Release.Name }}
Only explicitly enabled workloads are installed. Inspect with:
  kubectl --context <LAB_CONTEXT> -n {{ .Release.Namespace }} get sts,pods,pvc,svc

Redis clients MUST discover the writable primary via the three authenticated
{{ include "db-lab.fullname" . }}-redis-sentinel endpoints (port 26379), master name mymaster.
The headless redis Service is a peer-discovery endpoint, NOT a writable load balancer.

No plaintext passwords are bundled. Secrets are external and must already exist.
NetworkPolicy enforcement requires a supporting CNI. Clients in this namespace
need the label db-lab/client={{ .Release.Name }}.

After first-time ES/Galera bootstrap, seal bootstrapNewCluster=false BEFORE
loading data. Read helmchart/README.md for migration and full-outage recovery.
This is a learning chart, not an automatically recovering production HA operator.
