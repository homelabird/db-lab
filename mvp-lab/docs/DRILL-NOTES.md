# 한 번의 장애 실습 기록

수치와 로그는 직접 관측한 것만 기입합니다. “예상”과 “실제”를 별도 칸에 남깁니다.

## 실습 대상

날짜 / 호스트:
MVP project / image 태그 또는 digest:
실험 mode:
관찰할 주문 ID / 시작 version:
변경 명령:

## 변경 전 확인

`smoke` 결과:
주문 원본 상태와 version:
상세 source / cache:
검색 결과와 version:
컨테이너 상태 / outbox_pending / Kafka lag:

## 예상과 실제

| 기능 | 예상 | 실제 응답/지연/상태 코드 |
|---|---|---|
| 신규 주문 | | |
| 일반 상세 조회 | | |
| MariaDB 직접 조회 | | |
| 기존 주문 검색 | | |
| 새 주문 검색 | | |

## 원인 분리

문제가 생긴 것은 프로세스 / 이름 해석 / 포트 / 암호 / 데이터 / 처리 로직 중 무엇인가?
DB 직접 연결은 가능한가? worker가 실행 중인가?
마지막 정상 단계와 첫 실패 단계는 어디인가?
관련 order_id / event_id / request_id / SQL 오류 코드:
outbox와 consumer lag는 각각 어떻게 변했는가?

## 복구 확인

복구 명령:
정상 연결이 돌아온 시각:
대기 작업이 줄었는가:
같은 주문 ID / version이 검색에 반영됐는가:
재시도 때문에 주문/검색 문서가 중복됐는가:
`smoke` 재실행 결과:

## 교체와 데이터의 관계

바뀐 것은 컨테이너 / 이미지 버전 / 연결 주소 / 암호 / 볼륨 / 인덱스 중 무엇인가?
기존 데이터는 실제로 보존됐는가? 아니면 원본에서 재생성됐는가?
다음에는 어떤 한 가지 조건만 바꿔 비교할 것인가?

주의: 처음 실패 후 재생성된 Pod/컨테이너가 Running이 된 시간만으로 RTO/RPO를 주장하지 않습니다.
