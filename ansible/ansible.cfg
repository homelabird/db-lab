[defaults]
inventory = ./inventory/local.yml
library = ./library
module_utils = ./module_utils
host_key_checking = True
retry_files_enabled = False
interpreter_python = auto_silent
forks = 5
# No persistent log_path: native logs may contain private data.

[ssh_connection]
pipelining = True
