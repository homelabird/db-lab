# Copy to .env. KEY=value only; do not use shell commands or inline comments.
COMPOSE_PROJECT_NAME=es9-lab
COMPOSE_PROVIDER=docker
LAB_CLUSTER_NAME=es9-lab
# Client URL used by scripts running ON the lab host; not a listen address.
ES_URL=http://127.0.0.1:9201
# Unauthenticated lab by default: local access only.
# Remote access requires an isolated network AND ES_ALLOW_PUBLIC_BIND=yes.
ES_BIND_IP=127.0.0.1
ES_ALLOW_PUBLIC_BIND=no
# ES9 published ports; the legacy 7.x lab uses 9200/9000/5601, so the two labs
# can run side by side without a port conflict.
ES_PORT=9201
KIBANA_PORT=5602
ES_HEAP=640m

# X-Pack Security (opt-in). The default keeps the lab unauthenticated (HTTP).
# Setting XPACK_SECURITY_ENABLED=true also requires transport TLS on ES 8/9
# multi-node clusters; TLS is NOT configured by this lab (see README).
XPACK_SECURITY_ENABLED=false
ELASTIC_USERNAME=elastic
# ELASTIC_PASSWORD=set-me-when-enabled
# Kibana connects as kibana_system (the `elastic` superuser name is rejected by
# Kibana 9). With security disabled the username is effectively cosmetic.
KIBANA_USERNAME=kibana_system

# Shared filesystem snapshot repository, created and verified by ./lab.sh up.
# All 5 nodes mount the shared es-snapshots volume at this path.
SNAPSHOT_REPO_NAME=lab-snapshots
SNAPSHOT_PATH=/usr/share/elasticsearch/snapshots

WAIT_SECONDS=300