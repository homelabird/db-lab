services:
  es01:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.29
    container_name: cerebro-seed-es01
    environment:
    - node.name=es01
    - cluster.name=${LAB_CLUSTER_NAME:-cerebro-shard-lab}
    - discovery.seed_hosts=es01,es02,es03,es04,es05
    - cluster.initial_master_nodes=es01,es02,es03
    - node.attr.zone=zone-a
    - xpack.security.enabled=${XPACK_SECURITY_ENABLED:-false}
    - path.repo=/usr/share/elasticsearch/snapshots
    - ELASTIC_USERNAME=${ELASTIC_USERNAME:-elastic}
    - ELASTIC_PASSWORD=${ELASTIC_PASSWORD:-}
    - xpack.ml.enabled=false
    - ES_JAVA_OPTS=-Xms${ES_HEAP:-640m} -Xmx${ES_HEAP:-640m}
    - node.roles=master,data,ingest
    - node.processors=2
    - ingest.geoip.downloader.enabled=false
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - es01-data:/usr/share/elasticsearch/data
    - es-snapshots:/usr/share/elasticsearch/snapshots
    ports:
    - ${ES_BIND_IP:-127.0.0.1}:${ES_PORT:-9200}:9200
    networks:
    - es-lab
    entrypoint:
    - /bin/bash
    - -c
    - |
      mkdir -p /usr/share/elasticsearch/snapshots
      chown -R 1000:0 /usr/share/elasticsearch/snapshots
      exec /usr/local/bin/docker-entrypoint.sh "$@"
    - lab-bootstrap
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS --max-time 5 http://localhost:9200/_cluster/health >/dev/null
      interval: 10s
      timeout: 6s
      retries: 30
      start_period: 40s
  es02:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.29
    container_name: cerebro-seed-es02
    environment:
    - node.name=es02
    - cluster.name=${LAB_CLUSTER_NAME:-cerebro-shard-lab}
    - discovery.seed_hosts=es01,es02,es03,es04,es05
    - cluster.initial_master_nodes=es01,es02,es03
    - node.attr.zone=zone-b
    - xpack.security.enabled=${XPACK_SECURITY_ENABLED:-false}
    - path.repo=/usr/share/elasticsearch/snapshots
    - ELASTIC_USERNAME=${ELASTIC_USERNAME:-elastic}
    - ELASTIC_PASSWORD=${ELASTIC_PASSWORD:-}
    - xpack.ml.enabled=false
    - ES_JAVA_OPTS=-Xms${ES_HEAP:-640m} -Xmx${ES_HEAP:-640m}
    - node.roles=master,data,ingest
    - node.processors=2
    - ingest.geoip.downloader.enabled=false
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - es02-data:/usr/share/elasticsearch/data
    - es-snapshots:/usr/share/elasticsearch/snapshots
    networks:
    - es-lab
    entrypoint:
    - /bin/bash
    - -c
    - |
      mkdir -p /usr/share/elasticsearch/snapshots
      chown -R 1000:0 /usr/share/elasticsearch/snapshots
      exec /usr/local/bin/docker-entrypoint.sh "$@"
    - lab-bootstrap
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS --max-time 5 http://localhost:9200/_cluster/health >/dev/null
      interval: 10s
      timeout: 6s
      retries: 30
      start_period: 40s
  es03:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.29
    container_name: cerebro-seed-es03
    environment:
    - node.name=es03
    - cluster.name=${LAB_CLUSTER_NAME:-cerebro-shard-lab}
    - discovery.seed_hosts=es01,es02,es03,es04,es05
    - cluster.initial_master_nodes=es01,es02,es03
    - node.attr.zone=zone-c
    - xpack.security.enabled=${XPACK_SECURITY_ENABLED:-false}
    - path.repo=/usr/share/elasticsearch/snapshots
    - ELASTIC_USERNAME=${ELASTIC_USERNAME:-elastic}
    - ELASTIC_PASSWORD=${ELASTIC_PASSWORD:-}
    - xpack.ml.enabled=false
    - ES_JAVA_OPTS=-Xms${ES_HEAP:-640m} -Xmx${ES_HEAP:-640m}
    - node.roles=master,data,ingest
    - node.processors=2
    - ingest.geoip.downloader.enabled=false
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - es03-data:/usr/share/elasticsearch/data
    - es-snapshots:/usr/share/elasticsearch/snapshots
    networks:
    - es-lab
    entrypoint:
    - /bin/bash
    - -c
    - |
      mkdir -p /usr/share/elasticsearch/snapshots
      chown -R 1000:0 /usr/share/elasticsearch/snapshots
      exec /usr/local/bin/docker-entrypoint.sh "$@"
    - lab-bootstrap
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS --max-time 5 http://localhost:9200/_cluster/health >/dev/null
      interval: 10s
      timeout: 6s
      retries: 30
      start_period: 40s
  es04:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.29
    container_name: cerebro-seed-es04
    environment:
    - node.name=es04
    - cluster.name=${LAB_CLUSTER_NAME:-cerebro-shard-lab}
    - discovery.seed_hosts=es01,es02,es03,es04,es05
    - cluster.initial_master_nodes=es01,es02,es03
    - node.attr.zone=zone-a
    - xpack.security.enabled=${XPACK_SECURITY_ENABLED:-false}
    - path.repo=/usr/share/elasticsearch/snapshots
    - ELASTIC_USERNAME=${ELASTIC_USERNAME:-elastic}
    - ELASTIC_PASSWORD=${ELASTIC_PASSWORD:-}
    - xpack.ml.enabled=false
    - ES_JAVA_OPTS=-Xms${ES_HEAP:-640m} -Xmx${ES_HEAP:-640m}
    - node.roles=master,data,ingest
    - node.processors=2
    - ingest.geoip.downloader.enabled=false
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - es04-data:/usr/share/elasticsearch/data
    - es-snapshots:/usr/share/elasticsearch/snapshots
    networks:
    - es-lab
    entrypoint:
    - /bin/bash
    - -c
    - |
      mkdir -p /usr/share/elasticsearch/snapshots
      chown -R 1000:0 /usr/share/elasticsearch/snapshots
      exec /usr/local/bin/docker-entrypoint.sh "$@"
    - lab-bootstrap
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS --max-time 5 http://localhost:9200/_cluster/health >/dev/null
      interval: 10s
      timeout: 6s
      retries: 30
      start_period: 40s
  es05:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.29
    container_name: cerebro-seed-es05
    environment:
    - node.name=es05
    - cluster.name=${LAB_CLUSTER_NAME:-cerebro-shard-lab}
    - discovery.seed_hosts=es01,es02,es03,es04,es05
    - cluster.initial_master_nodes=es01,es02,es03
    - node.attr.zone=zone-b
    - xpack.security.enabled=${XPACK_SECURITY_ENABLED:-false}
    - path.repo=/usr/share/elasticsearch/snapshots
    - ELASTIC_USERNAME=${ELASTIC_USERNAME:-elastic}
    - ELASTIC_PASSWORD=${ELASTIC_PASSWORD:-}
    - xpack.ml.enabled=false
    - ES_JAVA_OPTS=-Xms${ES_HEAP:-640m} -Xmx${ES_HEAP:-640m}
    - node.roles=master,data,ingest
    - node.processors=2
    - ingest.geoip.downloader.enabled=false
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - es05-data:/usr/share/elasticsearch/data
    - es-snapshots:/usr/share/elasticsearch/snapshots
    networks:
    - es-lab
    entrypoint:
    - /bin/bash
    - -c
    - |
      mkdir -p /usr/share/elasticsearch/snapshots
      chown -R 1000:0 /usr/share/elasticsearch/snapshots
      exec /usr/local/bin/docker-entrypoint.sh "$@"
    - lab-bootstrap
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS --max-time 5 http://localhost:9200/_cluster/health >/dev/null
      interval: 10s
      timeout: 6s
      retries: 30
      start_period: 40s
  cerebro:
    image: docker.io/lmenezes/cerebro:0.9.4
    container_name: cerebro-seed-cerebro
    depends_on:
    - es01
    - es02
    - es03
    - es04
    - es05
    ports:
    - ${ES_BIND_IP:-127.0.0.1}:${CEREBRO_PORT:-9000}:9000
    volumes:
    - ./cerebro/application.conf:/opt/cerebro/conf/application.conf:ro,Z
    networks:
    - es-lab
    environment:
    - JAVA_TOOL_OPTIONS=-Xms128m -Xmx256m
  kibana:
    image: docker.elastic.co/kibana/kibana:7.17.29
    container_name: cerebro-seed-kibana
    depends_on:
    - es01
    - es02
    - es03
    - es04
    - es05
    ports:
    - ${ES_BIND_IP:-127.0.0.1}:${KIBANA_PORT:-5601}:5601
    networks:
    - es-lab
    environment:
    - SERVER_NAME=kibana
    - SERVER_HOST=0.0.0.0
    - ELASTICSEARCH_HOSTS=["http://es01:9200","http://es02:9200","http://es03:9200","http://es04:9200","http://es05:9200"]
    - XPACK_SECURITY_ENABLED=${XPACK_SECURITY_ENABLED:-false}
    - ELASTICSEARCH_USERNAME=${ELASTIC_USERNAME:-elastic}
    - ELASTICSEARCH_PASSWORD=${ELASTIC_PASSWORD:-}
    - TELEMETRY_ENABLED=false
networks:
  # Plain bridge only: no runtime-specific options, so both Docker Compose
  # and podman-compose can create this network (the Docker-only ICC bridge
  # option is rejected by podman-netavark, aborting podman-compose with exit 125).
  es-lab:
    driver: bridge
volumes:
  es01-data: null
  es02-data: null
  es03-data: null
  es04-data: null
  es05-data: null
  # One shared filesystem for the snapshot repository on every node.
  es-snapshots: null
