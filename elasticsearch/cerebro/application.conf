secret = "cerebro-shard-lab-change-me"
basePath = "/"
pidfile.path = "/dev/null"
rest.history.size = 100
data.path = "/tmp/cerebro.db"

play {
  server.http.port = 9000
  server.http.address = "0.0.0.0"
}

es = {
  gzip = true
}

hosts = [
  {
    host = "http://es01:9200"
    name = "Lab via es01 (default)"
  },
  {
    host = "http://es02:9200"
    name = "Lab via es02 (es01 down: select manually)"
  },
  {
    host = "http://es04:9200"
    name = "Lab via es04 (alternate)"
  }
]
